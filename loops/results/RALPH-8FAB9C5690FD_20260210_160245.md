# Resultado: RALPH-8FAB9C5690FD

**Status:** ✅ Completo
**Data:** 10/02/2026 16:02:45

---

Todos os 8 testes passaram. ✅

**Entregáveis:**

```python
# calcular_media.py
def calcular_media(notas: list[float]) -> float:
    """Calcula média aritmética com tratamento de erro."""
    if not isinstance(notas, list):
        raise TypeError("notas deve ser uma lista")
    
    if len(notas) == 0:
        return 0.0
    
    for nota in notas:
        if not isinstance(nota, (int, float)):
            raise ValueError("Todas as notas devem ser números")
    
    return sum(notas) / len(notas)
```

**Cobertura de testes:**
- ✅ Lista vazia → retorna 0.0
- ✅ Uma ou mais notas → média correta
- ✅ Números negativos e decimais
- ✅ Validação de tipo (lista)
- ✅ Validação de elementos numéricos

<RALPH_COMPLETE>
