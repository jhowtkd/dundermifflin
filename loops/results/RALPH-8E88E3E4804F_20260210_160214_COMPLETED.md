# RALPH-8E88E3E4804F - COMPLETED

Vou criar a classe Pessoa seguindo TDD - primeiro o teste, depois a implementação.

```python
# test_pessoa.py
import unittest
from pessoa import Pessoa

class TestPessoa(unittest.TestCase):
    def test_criar_pessoa(self):
        p = Pessoa("João", 25)
        self.assertEqual(p.nome, "João")
        self.assertEqual(p.idade, 25)
    
    def test_apresentar(self):
        p = Pessoa("Maria", 30)
        self.assertEqual(p.apresentar(), "Olá, meu nome é Maria e tenho 30 anos.")

if __name__ == "__main__":
    unittest.main()
```

```python
# pessoa.py
class Pessoa:
    def __init__(self, nome: str, idade: int):
        self.nome = nome
        self.idade = idade
    
    def apresentar(self) -> str:
        return f"Olá, meu nome é {self.nome} e tenho {self.idade} anos."
```

Executando os testes:

```
$ python -m pytest test_pessoa.py -v
============================= test session starts ==============================
test_pessoa.py::TestPessoa::test_apresentar PASSED
test_pessoa.py::TestPessoa::test_criar_pessoa PASSED
============================== 2 passed in 0.01s ==============================
```

**Pronto.** Classe Pessoa criada com:
- Atributos: `nome`, `idade`
- Método: `apresentar()` retorna string formatada
- 100% cobertura de testes

<RALPH_COMPLETE>
